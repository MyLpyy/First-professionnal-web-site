const API_ENDPOINT = "http://localhost:4001";

let userToken = localStorage.getItem('tokenn');

localStorage.setItem('token', JSON.stringify("dadAZDCREAR44arcazr"));

/* async function log() {
    if (userToken !== null) {
        if (userToken === ) {

        }
    }
}
log(); */

function test() {
    let container = document.querySelector("#login_button");
    container.innerHTML += `<a href="">
    <li>Login</li>
</a>`
} test();